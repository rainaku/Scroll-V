<Window x:Class="ScrollV.RunningAppsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Running Apps"
        Height="500" Width="380"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <!-- Close Button Style -->
        <Style x:Key="DialogCloseButton" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="Transparent" CornerRadius="6">
                            <TextBlock x:Name="icon" Text="✕" FontSize="11" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="#707070"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#40EF4444"/>
                                <Setter TargetName="icon" Property="Foreground" Value="#FF6B6B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#60EF4444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- App Item Style -->
        <Style x:Key="AppItemBorder" TargetType="Border">
            <Setter Property="Background" Value="#0CFFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="BorderBrush" Value="#15FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#12FFFFFF"/>
                    <Setter Property="BorderBrush" Value="#25FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Window Fade-in Animation -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.25">
                        <DoubleAnimation.EasingFunction>
                            <QuadraticEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetName="MainContainer"
                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                     From="20" To="0" Duration="0:0:0.3">
                        <DoubleAnimation.EasingFunction>
                            <QuadraticEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border x:Name="MainContainer" CornerRadius="20" Margin="10">
        <Border.RenderTransform>
            <TranslateTransform Y="0"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>
        <Border.Background>
            <SolidColorBrush Color="#E81a1a1f"/>
        </Border.Background>

        <Grid>
            <!-- Glass Overlay -->
            <Border CornerRadius="20" Opacity="0.04">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="White" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="0.4"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <DockPanel>
                <!-- Header -->
                <Border DockPanel.Dock="Top" Padding="20,16" Background="Transparent"
                        MouseLeftButtonDown="Header_MouseLeftButtonDown" Cursor="Hand">
                    <Grid>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="🔧" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock x:Name="TitleText" Text="Running Apps" FontSize="16" FontWeight="SemiBold" 
                                       Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Button Style="{StaticResource DialogCloseButton}" 
                                HorizontalAlignment="Right"
                                Click="CloseButton_Click"/>
                    </Grid>
                </Border>

                <!-- Separator -->
                <Border DockPanel.Dock="Top" Height="1" Background="#15FFFFFF" Margin="20,0"/>

                <!-- Search -->
                <Border DockPanel.Dock="Top" Margin="20,16,20,12">
                    <Border Background="#0CFFFFFF" CornerRadius="12" Padding="14,12"
                            BorderBrush="#18FFFFFF" BorderThickness="1">
                        <Grid>
                            <TextBlock Text="🔍" FontSize="14" Foreground="#707070" 
                                       VerticalAlignment="Center" Margin="0,0,0,0"/>
                            <TextBox x:Name="SearchBox" Background="Transparent" 
                                     BorderThickness="0" Foreground="White" FontSize="14"
                                     Margin="28,0,0,0" VerticalAlignment="Center"
                                     TextChanged="SearchBox_TextChanged">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="CaretBrush" Value="White"/>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                            <TextBlock x:Name="SearchPlaceholder" Text="Search apps..." 
                                       FontSize="14" Foreground="#606060" 
                                       Margin="28,0,0,0" VerticalAlignment="Center"
                                       IsHitTestVisible="False"/>
                        </Grid>
                    </Border>
                </Border>

                <!-- Hint -->
                <TextBlock x:Name="HintText" DockPanel.Dock="Top" Text="Toggle to exclude apps from smooth scrolling" 
                           FontSize="12" Foreground="#707070" Margin="24,0,24,16"/>

                <!-- Apps List -->
                <ScrollViewer Margin="20,0,20,20" VerticalScrollBarVisibility="Auto"
                              Style="{StaticResource ThinScrollViewer}">
                    <ItemsControl x:Name="AppsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource AppItemBorder}">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Width="36" Height="36" CornerRadius="8" 
                                                    Background="#15FFFFFF" Margin="0,0,12,0">
                                                <TextBlock Text="📦" FontSize="16" 
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding ProcessName}" FontSize="14" 
                                                           Foreground="White" FontWeight="Medium"/>
                                                <TextBlock Text="{Binding WindowTitle}" FontSize="11" 
                                                           Foreground="#707070" MaxWidth="180"
                                                           TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                        
                                        <CheckBox IsChecked="{Binding IsEnabled}" 
                                                  HorizontalAlignment="Right" VerticalAlignment="Center"
                                                  Style="{StaticResource GlassToggle}"
                                                  Checked="AppToggle_Changed" Unchecked="AppToggle_Changed"
                                                  Tag="{Binding ProcessName}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </Grid>
    </Border>
</Window>
